# fileInformation
### CMake (execute file)
comandi linux.

**Posizione di partenza**`./So2022`

digitare i comandi sotto elencati per eseguire il progetto:

``` bash
cd fileInfrmation
cd build
cmake ..
cmake --build .
./fielInformation
```